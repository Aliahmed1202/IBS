// Reports View - Handles reports interface
class ReportsView {
    constructor() {
        this.init();
    }

    init() {
        // Reports view is handled directly in ReportsController
        // This is a placeholder for future reports UI enhancements
    }

    renderReports(reportData) {
        // Handle reports rendering
        console.log('Reports rendered:', reportData);
    }
}

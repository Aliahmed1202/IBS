// Customer View - Handles customer interface
class CustomerView {
    constructor() {
        this.init();
    }

    init() {
        // Customer view is handled directly in CustomerController
        // This is a placeholder for future customer UI enhancements
    }

    renderCustomers(customers) {
        // Handle customers rendering
        console.log('Customers rendered:', customers.length);
    }
}

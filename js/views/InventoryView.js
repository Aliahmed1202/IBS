// Inventory View - Handles inventory interface
class InventoryView {
    constructor() {
        this.init();
    }

    init() {
        // Inventory view is handled directly in InventoryController
        // This is a placeholder for future inventory UI enhancements
    }

    renderProducts(products) {
        // Handle products rendering
        console.log('Products rendered:', products.length);
    }
}

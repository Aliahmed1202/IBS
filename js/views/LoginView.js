// Login View - Handles login interface interactions
class LoginView {
    constructor() {
        this.init();
    }

    init() {
        // Login view is handled directly in HTML and AuthController
        // This is a placeholder for future login UI enhancements
    }

    showLoginError(message) {
        // Handle login error display
        console.log('Login error:', message);
    }

    showLoginSuccess(message) {
        // Handle login success display
        console.log('Login success:', message);
    }
}
